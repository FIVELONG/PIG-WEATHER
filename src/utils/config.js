const host = 'https://www.imaginepig.com' // http://www.imaginepig.com:7001 http://localhost:7001

const conf = {
  host,
  login: `${host}/weapp/login`,
  user: `${host}/weapp/user`
}
export default conf
